protocol: rtfocuser_causal_temporal_finetune_v1
seed: 20260910
model:
  hidden: 32
  activation_checkpointing: true
train:
  clip_length: 4
  crop_size: 0
  resize: false
  clips_per_update: 2
  warmup_updates: 2000
  total_updates: 20000
  adapter_lr: 0.0001
  backbone_lr: 0.00001
  min_lr_ratio: 0.1
  weight_decay: 0.0001
  temporal_weight: 0.01
  temporal_ramp_updates: 2000
  clip_grad: 1.0
  precision: bf16
  workers_per_rank: 2
  cpu_threads_per_rank: 2
  log_every: 20
  save_every: 500
  validate_every: 1000
  cut_threshold: 0.30
validation:
  frames_per_sequence: 32
  max_sequences_per_domain: 2
  max_psnr_drop_db_per_domain: 0.2
  minimum_valid_flow_fraction: 0.05
